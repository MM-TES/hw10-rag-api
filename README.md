# HW10 RAG API

Production RAG API for The Twelve-Factor App methodology.

FastAPI + SSE streaming, semantic cache (Qdrant), token-bucket rate limiting
(Upstash Redis REST), multi-provider LLM fallback (OpenRouter), cost tracking
(Supabase Postgres), prompt-injection defense, Langfuse v4 observability.

See `CLAUDE.md` for project context and `../README.md` for full ТЗ.
